server:
  port: 8763
spring:
  application:
    name: controller
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/
    fetch-registry: true
    register-with-eureka: true

