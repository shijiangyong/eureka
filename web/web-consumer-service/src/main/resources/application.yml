server:
  port: 8764
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/
#spring:
  #application:
   # name: resttemplate-ribbon
spring:
  application:
    name: consumer-feign